{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "NODE_ENV=development pnpm install --frozen-lockfile && pnpm backend:build && cd apps/backend/.medusa/server && npm install --omit=dev --no-audit --no-fund"
  },
  "deploy": {
    "startCommand": "pnpm backend:deploy",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
